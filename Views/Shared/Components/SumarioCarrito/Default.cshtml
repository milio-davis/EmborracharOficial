@model CarritoViewModel

@if (Model.Carrito.ItemsCarrito.Count > 0)
{
    <li>
        <a asp-controller="Carrito" asp-action="Index">
    <span class="glyphicon glyphicon-shopping-cart"></span>
    <span id="cart-status">
        @Model.Carrito.ItemsCarrito.Count
    </span>
    </a>
    </li>
}